# ------------------------------------------------------------
# Variáveis padrão para desenvolvimento local
# Renomeie este arquivo para `.env` e, se precisar de credenciais
# reais, adicione-as em `.env.local` (não versionado).
# ------------------------------------------------------------

# Flask
FLASK_APP=run.py
FLASK_ENV=development
FLASK_DEBUG=1
FLASK_CONFIG=development

# Credenciais obrigatórias
SECRET_KEY=your-secret-key-change-this-in-production
MONGO_URI=mongodb://localhost:27017/client_manager

# Aplicação
PORT=5000

# Rate limiting (opcional)
RATELIMIT_ENABLED=true
RATELIMIT_STORAGE_URI=memory://
RATELIMIT_STORAGE_URL=memory://  # compatibilidade legada

# Heleket payouts (preencha apenas em ambiente local; produção usa cofre de segredos)
HELEKET_PROJECT_URL=http://localhost:5000/payouts
HELEKET_MERCHANT_ID=00000000-0000-0000-0000-000000000000
HELEKET_API_KEY=your-heleket-api-key

# Segurança adicional (opcional)
BCRYPT_LOG_ROUNDS=12
JWT_SECRET_KEY=jwt-secret-key-change-in-prod

# Serviços externos (preencher somente em `.env.local` ou cofre de segredos)
GEMINI_API_KEY=your-gemini-api-key

# Scripts de deploy AWS (opcional)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_DEFAULT_REGION=us-east-1

# Outras integrações (adicione conforme necessário)
# EXAMPLE_API_URL=https://api.example.com
# EXAMPLE_API_KEY=your-example-key